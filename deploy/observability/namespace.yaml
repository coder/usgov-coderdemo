# Dedicated namespace for the in-cluster observability stack
# (kube-prometheus-stack: Prometheus + Grafana + the Prometheus operator).
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    app.kubernetes.io/part-of: usgov-coderdemo
    app.kubernetes.io/component: observability

# =============================================================================
# ClusterSecretStore + ExternalSecrets: sync usgov-coderdemo/* from AWS Secrets
# Manager into the app namespaces' Kubernetes Secrets.
# =============================================================================
# The ClusterSecretStore authenticates to ASM via the ESO controller's IRSA
# ServiceAccount (external-secrets/external-secrets), so no static AWS keys.
# Each ExternalSecret extracts the JSON keys of one ASM secret into a target
# Kubernetes Secret with the SAME name/keys the apps already reference, so the
# control-plane manifests (deploy/coder, deploy/keycloak, deploy/gitlab) are
# unchanged. ASM is the source of truth; ESO owns these Secrets and refreshes
# them hourly.
# -----------------------------------------------------------------------------
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secretsmanager
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-gov-west-1
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-db
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-db
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/db
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-oidc
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-oidc
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/oidc
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-ai
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-ai
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/ai
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-external-auth
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-external-auth
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/external-auth
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-provisioner-alpha
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-provisioner-alpha
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/provisioner-alpha
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-provisioner-bravo
  namespace: coder
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: coder-provisioner-bravo
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/coder/provisioner-bravo
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-admin
  namespace: keycloak
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: keycloak-admin
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/keycloak/admin
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-db
  namespace: keycloak
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: keycloak-db
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/keycloak/db
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-secrets
  namespace: gitlab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: gitlab-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: usgov-coderdemo/gitlab/secrets

# Out of scope (v1)

Do not implement unless orchestrator explicitly expands scope.

- RHAIIS / vLLM / GPU MachineSets
- GitLab HA (Gitaly cluster)
- Harbor / JFrog / self-hosted registry
- Istio sidecar injection in workspace pods
- OSSM on OCP (optional later)
- FIPS Istio builds (document only unless mandated)
- Air-gap / C2S / full ATO controls
- GitLab issue → workspace bridge (optional)
- EC2 workspace template (optional)
